# 工具与依赖

| 工具 | 用途 | 备注 |
|------|------|------|
| Node.js | 单测、sync、round 引擎 | 系统已装 |
| Git | 本地版本 | 不自动 push |
| Imagine / image_gen | 插画 | 可能配额失败，有降级 |
| clawhub | 可选 skill | 按需 |

无强制 npm 依赖（`package.json` 仅 scripts）。
