# Authentic development rounds (source of truth for acceptance)

Only rounds with **authentic:true** count. Criteria:
- Shipped runtime/assets change of real scope (~20–40 min unit)
- Distinct player-facing content or real system behavior (not `…#N` spam)
- Unique art bytes if art round (no duplicate MD5 copy-as-new)
- CSS changes must leave lasting distinct behavior (not stack-overwrite only)
- Not progress/docs-only (docs may accompany a shipped round but cannot be sole increment for game-integrated count)

Prior bulk `Ship … #N` / `Wire real stage art` / CSS `--r-accent` spam archived in `archive/rounds-template-spam-2026-07-24.jsonl` and **do not count**.

| Round | Status | Type | Goal | Outcome | Commit |
|------:|--------|------|------|---------|--------|
